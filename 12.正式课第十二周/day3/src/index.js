// createStore : state   getState(获取)  dispatch(reducer修改状态)   subscribe (订阅)
// combineReducers :
/*let  action ={
    add(){
        return {}
    }
}*/
// react-redux : 将状态和方法放到了当前组件的属性上；
// Provider  connect  : 高阶组件；
// bindActionCreators ==> 函数
/*
export default connect((state)=>({...state.counter}),action)()

// todo :*/

import "./system/index";
